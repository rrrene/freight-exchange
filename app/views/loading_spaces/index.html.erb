<div class="box">
  <h3>
    <%= t("loading_spaces.index.header") %>
    <%= link_to t("loading_spaces.common.new_link"), new_freight_path, :class => 'new' %>
  </h3>
  <p>
    <%= t('loading_spaces.index.intro') %>
  </p>

  <table>
    <thead>
      <tr>
        <th><%= t('loading_spaces.index.table.headers.date') %></th>
        <th><%= t('loading_spaces.index.table.headers.origin') %></th>
        <th><%= t('loading_spaces.index.table.headers.destination') %></th>
        <th class="options"><%= t('loading_spaces.index.table.headers.options') %></th>
      </tr>
    </thead>
    <tbody>
      <% collection.each do |ar| %>
        <tr>
          <td><%= link_to ar.origin_site_info.date, ar %></td>
          <td><%= ar.origin_site_info.name %></td>
          <td><%= ar.destination_site_info.name %></td>
          <td class="options">
            <%= link_to t('loading_spaces.index.table.options.edit'), {:action => 'edit', :id => ar}, :class => 'edit' %> 
            |
            <%#= link_to t('freights.index.table.options.delete'), {:action => 'destroy', :id => ar}, :class => 'destroy' %>
            <%= link_to t('loading_spaces.index.table.options.template'), new_resource_url(:parent_id => ar.id), :class => 'edit' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>