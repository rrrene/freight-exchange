<div id="sidebar" class="span-8 last">
  <% if logged_in? && current_user.is?(:company_admin) %>
    <p>
      <%= link_to t("common.create_new_posting"), "#", :class => 'create_new_posting' %>
    </p>
  <% end %>
  <%= yield(:sidebar) %>
</div>